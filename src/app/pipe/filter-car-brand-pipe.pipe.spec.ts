import { FilterCarBrandPipePipe } from './filter-car-brand-pipe.pipe';

describe('FilterCarBrandPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarBrandPipePipe();
    expect(pipe).toBeTruthy();
  });
});
